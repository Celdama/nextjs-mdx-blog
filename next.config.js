/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['imgs.search.brave.com', 'd24f1whwu8r3u4.cloudfront.net'],
  },
};

module.exports = nextConfig;
